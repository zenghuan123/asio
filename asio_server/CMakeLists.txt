
LIST(APPEND INCLUDE_PATH ${ASIO_PATH})
LIST(APPEND INCLUDE_PATH ${CMAKE_CURRENT_SOURCE_DIR}/include)

FILE(GLOB_RECURSE SRC ${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp)

ADD_LIBRARY(asio_server ${SRC})

TARGET_INCLUDE_DIRECTORIES(asio_server PUBLIC  ${INCLUDE_PATH})





